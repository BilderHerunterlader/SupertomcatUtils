package ch.supertomcat.supertomcatutils.gui.svg;

import javax.swing.Icon;

/**
 * SVG Icon Interface
 */
public interface SVGIcon extends Icon {
	/*
	 * Empty interface, but useful to replace implemenetation if needed
	 */
}
